# 三門課的精簡原則與銜接設計

> **這份給誰看：** 後端課 `AI_CRUD_Workshop_Backend_EASY` 與測試課 `AI_CRUD_Workshop_QATest_EASY` 的維護者（人或 AI）。
> **這份要你做什麼：** 照第二章的五個檢查法把你那門課也精簡一次，再照第五章把接縫對上前端課。
> **依據：** 前端課 `AI_CRUD_Workshop_Frontend_EASY` 於 2026-08-19 做完一輪精簡，本文是它的做法與踩到的坑。

---

## 一、精簡的唯一判準

**一份文件如果學員照手冊走永遠不會打開，它就不該存在。**

不是「寫得好不好」，不是「有沒有用」，是**有沒有入口**。沒有入口的好文件，跟不存在一樣——但它會製造三種傷害：

1. 學員瀏覽資料夾時被干擾，不確定自己是不是漏讀了什麼。
2. 它會跟手冊講的話慢慢不一致，變成錯誤來源。
3. 維護時要改兩個地方，而你一定只會改到一個。

---

## 二、五個檢查法（照順序做，都可以用指令跑）

### 檢查法 1：找出沒有入口的檔案

列出所有教材文件，逐一問「手冊有沒有點名它」。

```bash
# 把 HANDBOOK 換成你那門課的單一入口文件
for f in $(find . -name "*.md" -not -path "./.git/*" -not -path "*/node_modules/*"); do
  base=$(basename "$f")
  n=$(grep -c "$base" HANDBOOK/HANDBOOK.html 2>/dev/null || echo 0)
  [ "$n" = "0" ] && echo "沒有入口：$f"
done
```

**前端課的結果：10 個檔案、768 行，學員永遠打不開。**

處理原則：**先收精華，再刪舊檔。** 不要直接刪——這些檔案通常是舊動線的完整教材，裡面常有手冊在「收斂成單一入口」時漏掉的東西。前端課就在裡面撿回三塊：一張評量量表、一張 A/B 實測計分表、一份對照表少掉的六個項目。

### 檢查法 2：找出同名兩份

```bash
find . -name "*.md" -not -path "./.git/*" -not -path "*/node_modules/*" -exec md5sum {} \; \
  | sort | uniq -w32 -d
```

**前端課的結果：5 組重複。**其中最危險的是 harness 規則檔——同名、內容只差路徑寫法，學員與 AI 都可能讀到不生效的那一份，改了也沒用。

處理原則：**只留一份實檔，其他改成指向它的一行說明。** 判斷哪一份是實檔的方法：**AI 實際會讀到的那一份**（在專案根目錄、會被複製走的那份），不是教材展示用的那份。

### 檢查法 3：找出與本課無關的材料

問一句：**這份材料，本課的哪一個步驟會用到？** 答不出來就是無關。

**前端課的結果：一整包 49 個檔案的 ASP.NET harness 參考包被放在 step2。** 它被標成「課後附錄，回公司才用」，但學員上課時就會看到，而且它的技術棧（Vite／Pinia／axios／EF Core）跟課程用的 Nuxt 完全對不上。

處理原則：**無關就整包移除，不要用「課後附錄」當藉口留著。** 學員需要的是課後有東西可帶走，不是課中被干擾。真的要給，寫一份「回公司怎麼做」的方法說明，比塞一包程式碼有用。

> ⚠️ **這一項最容易做錯：** 移除前先查證它是不是真的無關。前端課原本以為那包跟範本有「文件格式對應關係」，列了 8 組對照，逐條查證後**只有 4 組成立**，另外 4 組是想像出來的。

### 檢查法 4：檢查對照基準有沒有過期

教材裡的「A 版 vs B 版」「以前 vs 現在」這類對照，**基準會過期**。

**前端課的結果：step1 的「有 harness vs 沒 harness」有 12 項對照，其中 8 項在比外觀**（顏色亂配、中英夾雜、必填沒標示、錯誤紅字、間距字級）。那是舊模型才會犯的錯。用現在的模型實跑一次，沒 harness 那版**做得又快又漂亮**，還自己建了一套 design token——學員一看就覺得這個對照不公平，harness 的說服力反而被削弱。

處理原則：**拿現在的模型實跑一次，用實測結果重寫對照。** 前端課重跑後把 12 項收成 3 項：

| 觀察點 | 沒 harness | 有 harness |
|---|---|---|
| 顏色從哪來 | 自己發明一套 token，你們的品牌色 0 次 | 用你們給的 token 與色票 |
| 業務規則從哪來 | 自己定驗證規則 | 具名常數，照你們的規格 |
| 多出來的東西 | **兩版都會自己多做** | harness 管「怎麼寫」，不管「做多少」 |

第三列是誠實揭露，反而最有價值：它承認 harness 的邊界，並把「範圍怎麼管」的球接給下一個步驟。

### 檢查法 5：檢查全庫殘留

每刪一批東西，一定要掃殘留引用。

```bash
grep -rn "被刪掉的檔名\|被刪掉的資料夾名" --include="*.md" --include="*.html" . | grep -v node_modules
```

**前端課的教訓：** 第一次掃用了關鍵字「參考包」，漏掉一句寫「後端另有 harness **參考**可比照」的殘留。**用檔名和資料夾名去掃，不要用你以為的說法去掃。**

---

## 三、後端課（Backend_EASY）要做的事

### 3.1 先做上面五個檢查法

特別注意檢查法 3：**後端課裡有沒有塞前端材料？** 如果有整包 Nuxt 範本或 Design System，同樣的道理——移除。

### 3.2 對照基準也要重跑（檢查法 4）

後端課如果有「有 harness vs 沒 harness」的 A/B，同樣要用現在的模型重跑。可預期的結果與前端課類似：

1. 沒 harness 的版本**也會寫出能跑的 API**，甚至自己加了 Swagger 註解、全域例外處理、DTO 分層。
2. 但它會**自己發明**：回應格式（不是你們的 `ApiResponse<T>`）、錯誤碼、命名慣例、資料夾結構。
3. 而且它同樣**擋不住多做**——會自己加你沒要的端點、快取、日誌。

建議的三項對照：**回應格式從哪來、錯誤處理規則從哪來、多出來的端點**。

### 3.3 接住前端課留的接縫（重點）

前端課的問卷（`step6_survey`）刻意做成純前端，資料存 `localStorage`，並在資料層留了一行：

```ts
// TODO: DEV 串接 POST /api/survey
```

**後端課的第一個練習，就應該是把這一行變成真的。** 建議做法：

| # | 後端課的步驟 | 對應前端課的什麼 |
|---|---|---|
| 1 | 拿前端課的 `PRD-課程回饋問卷.md`（8 個欄位、7 條行為規則） | 需求不用重寫，直接沿用 |
| 2 | 從那份 PRD 生出 SRS／SDD、資料表、API contract | 前端課教的「PRD → 釐清選擇題 → SRS/SDD」流程 |
| 3 | 實作 `POST /api/survey`、`GET /api/survey` | 前端那一行 TODO |
| 4 | 把前端的 composable 內部實作換成 `useFetch`，**呼叫端一行不改** | 前端課刻意留的「後端就緒」設計 |

**這件事的教學價值：** 學員會親眼看到「前端留接縫」不是空話——真的換的時候，頁面一行都沒動。

### 3.4 後端 harness 四件要跟前端對齊格式

前端課的四件是：專案憲法、程式規則細則、Design System 摘要、複製範本使用說明。
後端課應該是同樣的骨架、不同的內容：**專案憲法、程式規則細則（API／EF Core）、資料庫與回應格式規範、複製範本使用說明。**

**四件的實檔要放在專案根目錄**（AI 會自動讀的位置），教材資料夾**不要放第二份副本**。這是前端課踩過的坑。

---

## 四、測試課（QATest_EASY）要做的事

### 4.1 先做上面五個檢查法

### 4.2 受測對象直接用前面兩門課的產出

**不要另外準備一個受測 App。** 用學員自己在前端課做的問卷、在後端課接的 API——這樣測試課的紅燈是真的紅燈，不是刻意設計的假題目。

| 測什麼 | 用哪一課的產出 |
|---|---|
| E2E（UI 流程） | 前端課的問卷頁 `/survey`、`/survey/admin` |
| API 測試 | 後端課的 `POST /api/survey`、`GET /api/survey` |
| 資料驗證 | PRD 的 7 條行為規則就是現成的測試案例 |

### 4.3 前端課的 LOOP 已經教過的，不要重教

前端課的 step4 已經教了：

1. LOOP 是什麼（改 → 驗 → 再改）。
2. 兩次停損鐵律。
3. 不准為了讓燈變綠而放寬斷言、跳過測試。
4. 換第二個 AI 做對抗審查。
5. 失敗截圖診斷。

**測試課要接著往下講，不是重講。** 建議往這些方向：測試分級（L0 typecheck／L1 單元／L2 API smoke／L3 E2E）、測試資料管理、測試互相污染、假綠的辨識、覆蓋率的誤用。

### 4.4 一個誠實的提醒

前端課的 step4 有一句話值得抄過去：

> 這 7 條測試是**功能驗證＋失敗截圖診斷**，不是 pixel 比對的視覺回歸。
> **真正的視覺審查＝人逐張看截圖**，不是靠自動化斷言能取代的。

測試課最容易犯的錯，是讓學員以為「測試全綠 = 品質好」。要明講自動化測不到什麼。

---

## 五、三門課的銜接設計（一條資料流走到底）

```
前端課 Frontend_EASY
  step1  為什麼要 harness          ← 規矩的價值
  step2  範本 ＋ harness 四件       ← 骨架與規矩
  step3  PRD → 選擇題 → 生成新模組   ← 生成流程
  step4  LOOP ＋ E2E 驗證           ← 驗收紀律
  step6  【驗收題】做問卷並發布上線   ← 一個人獨立完成
         產出：問卷前端，資料存 localStorage
         留下：// TODO: DEV 串接 POST /api/survey
                    │
                    ▼
後端課 Backend_EASY
  沿用同一份 PRD-課程回饋問卷.md
  生出 SRS／SDD → 資料表 → API contract
  實作 POST /api/survey、GET /api/survey
  把前端 composable 內部換成 useFetch  ← 呼叫端一行不改
         產出：真的能收資料的問卷
                    │
                    ▼
測試課 QATest_EASY
  對前端問卷寫 E2E（填答、驗證、匯出、清空）
  對後端 API 寫 L2 smoke（正常、400、403）
  PRD 的 7 條行為規則 = 現成測試案例
         產出：三門課做出來的東西，有測試守著
```

### 三門課共用的東西

| 共用什麼 | 說明 |
|---|---|
| **同一份 PRD** | `step6_survey/PRD-課程回饋問卷.md`。三門課都拿它當需求來源，不要各寫一份 |
| **同一套 harness 骨架** | 四件的**名字與角色**一致，內容各自換成該課的技術棧 |
| **同一套心法** | PRD → 釐清選擇題 → SRS/SDD → 生成 → LOOP 驗證 |
| **同一條鐵律** | 兩次停損、不准作弊、審寫分離 |

### 三門課各自負責的邊界

| 課 | 管什麼 | 不管什麼 |
|---|---|---|
| 前端 | 畫面、互動、前端驗證、視覺一致性 | 資料真的存到哪、跨使用者的資料 |
| 後端 | 資料模型、API 契約、伺服器端驗證、持久化 | 畫面長什麼樣 |
| 測試 | 驗收標準可執行化、紅綠燈、假綠辨識 | 功能怎麼實作 |

---

## 六、做完的驗收標準

三門課各自照這張表自檢：

- [ ] 每一份留下來的文件，都能講出「學員在哪一步會打開它」。
- [ ] 全庫沒有同名兩份不同內容的規則檔。
- [ ] 沒有跟本課技術棧無關的材料。
- [ ] A/B 對照是用**現在的模型**實跑出來的，不是沿用舊結論。
- [ ] 刪除後掃過殘留引用，用**檔名與資料夾名**掃，不是用說法掃。
- [ ] 手冊漏掉的精華已經收回來，才刪舊檔。
- [ ] 接縫對得上：前端留的 TODO、後端接的 API、測試測的端點，三邊名稱一致。
- [ ] 三門課的 PRD 是同一份，不是各寫各的。

---

## 七、前端課這次的實際成果（給你當基準）

| 項目 | 精簡前 | 精簡後 |
|---|---|---|
| step2 資料夾數 | 5 | 2 |
| step2 頂層文件 | 12 | 5 |
| 同名重複檔 | 5 組 | 0 |
| 沒有入口的孤兒檔 | 10 個、768 行 | 0 |
| 與本課無關的材料 | 49 個檔案 | 0 |
| step1 對照項目 | 12 項（8 項在比外觀） | 3 項（全部可查證） |
| 評量表在學員動線上 | ✗ | ✓ |

**淨變化：刪掉約 3400 行，補回約 200 行。** 補回的那 200 行是原本漏掉的精華——評量量表、實測數據、誠實揭露 harness 的邊界。

**精簡不是把東西變少，是把「學員會打開的東西」變準。**

---

## 附錄 A：後端課的起手提示詞（整段複製給 AI）

> 在 `AI_CRUD_Workshop_Backend_EASY` 資料夾下開 AI Agent（要能讀檔改檔），整段貼上。

```
你是 AI CRUD 工作坊「後端 EASY 版」的教材維護者。工作目錄就是這個 repo。

請先完整讀專案根目錄的 `課程系列-精簡與銜接指引.md`。那是前端課
（AI_CRUD_Workshop_Frontend_EASY）剛做完一輪精簡的做法與踩到的坑，你要照它做。

照下面三個階段執行。每個階段做完就停下來回報，不要一路做到底。

【第一階段：盤點，這階段不准動任何檔案】

1. 照指引第二章「檢查法 1」，列出所有 HANDBOOK 沒有點名的教材文件。
   已知線索（我先查過的，你要驗證並補齊）：根目錄的 BUILD_LOG.md、
   COURSE_REVIEW_TEACHER_STUDENT.md、EVALUATION_REPORT_2026-07-15.md 共 653 行，
   兩份 HANDBOOK 都沒有提到它們。
2. 照「檢查法 2」找同名兩份。
   已知線索：HANDBOOK/ 底下同時有 HANDBOOK.html（776 行）與 HANDBOOK.md（481 行）。
   請確認兩份內容差多少、學員實際會開哪一份、有沒有已經不一致的地方。
3. 照「檢查法 3」找與本課無關的材料。特別檢查有沒有整包前端範本、Design System
   或其他技術棧的參考包塞在 step 資料夾裡。
4. 照「檢查法 4」檢查 step1 的「有 harness vs 沒 harness」對照。
   如果對照項目是在比「程式醜不醜、有沒有寫註解」這類外觀，它多半已經過期——
   現在的模型不給規矩也寫得出漂亮的 API。請實跑一次驗證再下判斷。

盤點完交一張表：檔案、行數、HANDBOOK 有沒有點名、建議留或刪、理由。
**停下來等我確認。**

【第二階段：我確認後才執行】

5. 先收精華再刪舊檔。刪任何一份之前，確認裡面沒有 HANDBOOK 漏掉的內容——
   前端課就在要刪的檔案裡撿回一張評量量表和一張實測對照表。
6. 每刪一批，照「檢查法 5」用**檔名與資料夾名**掃殘留引用，不要用你以為的說法去掃。
7. 兩份 HANDBOOK 只留一份，並確認全庫沒有還指向被刪那份的連結。

【第三階段：接住前端課的接縫】

8. 讀根目錄的 `PRD-課程回饋問卷.md`。那是前端課課後驗收題的需求，
   學員已經做出前端問卷，資料存在瀏覽器，程式裡留了一行
   `// TODO: DEV 串接 POST /api/survey`。
9. 照指引第 3.3 節，設計一個練習：把那一行變成真的 API。要求：
   - 沿用同一份 PRD，不要另外寫需求。
   - 走 PRD → 釐清選擇題 → SRS/SDD → 生成 → LOOP 驗證的流程。
   - 端點名稱與前端留的那一行一致（POST /api/survey、GET /api/survey）。
   - 前端只換 composable 的內部實作，呼叫端一行都不改——這是教學重點，要驗證得出來。
10. 檢查後端 harness 四件是不是放在專案根目錄（AI 會自動讀的位置）。
    教材資料夾裡不要放第二份副本，那會讓人改到不生效的那一份。

鐵律：
- 同一個問題連續修兩次還沒好，停下來回報，不要無限撞牆。
- 每一份你建議刪除的檔案，都要能講出「學員在哪一步會打開它」，講不出來才刪。
- 不准為了讓數字好看而刪掉學員真的會用到的東西。
```

---

## 附錄 B：測試課的起手提示詞（整段複製給 AI）

> 在 `AI_CURD_Workshop_QATest_EASY` 資料夾下開 AI Agent（要能讀檔改檔），整段貼上。

```
你是 AI CRUD 工作坊「QA 測試 EASY 版」的教材維護者。工作目錄就是這個 repo。

請先完整讀專案根目錄的 `課程系列-精簡與銜接指引.md`。那是前端課
（AI_CRUD_Workshop_Frontend_EASY）剛做完一輪精簡的做法與踩到的坑，你要照它做。

照下面三個階段執行。每個階段做完就停下來回報。

【第一階段：盤點，這階段不准動任何檔案】

1. 照指引第二章「檢查法 1」，列出所有 HANDBOOK 沒有點名的教材文件。
   本課只有約 50 個追蹤檔，已經算精簡，重點放在「有沒有入口」而不是「數量多不多」。
2. 照「檢查法 2」找同名兩份與功能重疊。
   已知線索：根目錄有 `導讀地圖.md`（99 行），HANDBOOK/ 有 `HANDBOOK.md`（1033 行）。
   兩份都在做導覽。請確認它們是否互相矛盾、學員該看哪一份、能不能併成一份。
3. 照「檢查法 3」找與本課無關的材料，包括 extras/ 底下的東西。
4. 照「檢查法 4」檢查課程裡任何「以前 vs 現在」「好 vs 壞」的對照是否過期。

盤點完交一張表：檔案、行數、HANDBOOK 有沒有點名、建議留或刪、理由。
**停下來等我確認。**

【第二階段：我確認後才執行】

5. 先收精華再刪舊檔。刪之前確認裡面沒有 HANDBOOK 漏掉的內容。
6. 每刪一批，照「檢查法 5」用檔名與資料夾名掃殘留引用。

【第三階段：銜接前面兩門課】

7. 讀根目錄的 `PRD-課程回饋問卷.md`。它的第 4 節有 7 條行為規則（BR-1 到 BR-7）
   與第 5 節 5 條非功能需求——**那些就是現成的測試案例**，不用另外編題目。
8. 目前本課有 `system_under_test/`，裡面是自備的 vendor-api 與 vendor-app。
   指引第 4.2 節建議改用前兩門課的產出當受測對象（前端課學員做的問卷、
   後端課接的 API），這樣紅燈是真的紅燈，不是刻意設計的假題目。
   **這是重大結構調整，先評估這三件事再給我建議，不要直接動手：**
   - 改用學員自己的產出，會不會造成「每個學員的受測系統長得不一樣」而難以出題？
   - 自備 SUT 的好處是可控（可以 inject-bugs）。改掉之後怎麼補這個好處？
   - 有沒有折衷做法：主線用學員自己的產出，inject-bugs 那段仍用自備 SUT？
9. 照指引第 4.3 節檢查有沒有重教前端課已經教過的東西。前端課 step4 已經教了：
   LOOP 是什麼、兩次停損、不准放寬斷言或跳過測試、換第二個 AI 對抗審查、失敗截圖診斷。
   本課應該往下講：測試分級（L0 typecheck／L1 單元／L2 API smoke／L3 E2E）、
   測試資料管理、測試互相污染、假綠的辨識、覆蓋率的誤用。
10. 加一段誠實提醒，講清楚自動化測不到什麼。可參考前端課的寫法：
    「這些測試是功能驗證＋失敗截圖診斷，不是 pixel 比對的視覺回歸。
      真正的視覺審查＝人逐張看截圖，不是靠自動化斷言能取代的。」

鐵律：
- 同一個問題連續修兩次還沒好，停下來回報。
- 每一份你建議刪除的檔案，都要能講出「學員在哪一步會打開它」，講不出來才刪。
- 第 8 項是結構調整，只准給建議，不准自己動手改。
```

---

## 附錄 C：前端課的參考位置

- Repo：`AI_CRUD_Workshop_Frontend_EASY`（GitHub：`xian711/AI_CRUD_Workshop_Frontend_EASY`）
- 精簡的完整過程在 git log，關鍵幾筆：
  - `48a7552` step2 整併：5 個資料夾收成 3 個
  - `73a45f3` 移除與本課無關的 asp.net 參考包
  - `23ca4c2` 收回 HANDBOOK 漏掉的精華，刪 6 個沒有入口的檔
  - `ce5f30d` step1 對照從「醜 vs 美」改成「誰做的決定」，demo 換成實測產出
- 課後驗收題與問卷 PRD：`step6_survey/`
