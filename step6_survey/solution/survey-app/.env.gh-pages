# GitHub Pages 發布用環境變數（給 pnpm generate:gh 讀，nuxt.config 的 app.baseURL 會吃這個值）
#
# 專案頁（Project Pages）網址長這樣：https://<你的 GitHub 帳號>.github.io/<repo 名稱>/
# 所以這裡要填「/<repo 名稱>/」，前後的斜線都不能少。
# 例：repo 叫 course-survey → NUXT_APP_BASE_URL=/course-survey/
#
# 如果是使用者頁（https://<帳號>.github.io/，repo 名叫 <帳號>.github.io），改成 /
NUXT_APP_BASE_URL=/survey-app/
