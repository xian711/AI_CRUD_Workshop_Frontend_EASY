# PRD — 課程回饋問卷（課後驗收題）

> 一頁需求。你要用這門課學到的東西，把它變成一個能上線的網頁。
> 這份 PRD 刻意只寫「要什麼」，不寫「怎麼做」——怎麼做是你跟 AI 的事。

## 1. 這是什麼

課程結束後要收學員回饋。需要一個**可以公開發布的問卷網頁**，學員用手機或電腦打開就能填。

本版**沒有後端**。回覆先存在填答者自己的瀏覽器裡，主辦方可以在管理頁把全部回覆匯出成 CSV。
接後端是下一門課的事，所以程式裡要留好接縫。

## 2. 兩個頁面

| 路由 | 給誰用 | 做什麼 |
|---|---|---|
| `/survey` | 學員（填答者） | 填問卷、送出、看到成功訊息 |
| `/survey/admin` | 主辦方 | 看所有回覆、匯出 CSV、清空 |

## 3. 問卷欄位（8 題）

| # | 欄位 | 型別 | 必填 | 說明 |
|---|---|---|:---:|---|
| 1 | 姓名 | 文字 | — | 選填，可匿名 |
| 2 | 服務單位 | 文字 | ✅ | 一般文字，上限 30 字 |
| 3 | 整體滿意度 | 1–5 | ✅ | 5 = 非常滿意 |
| 4 | 最有收穫的段落 | 單選 | ✅ | 為什麼要 harness／範本與規矩／生成新模組／LOOP 驗證 |
| 5 | 課程難度 | 單選 | ✅ | 太簡單／剛好／太難 |
| 6 | 回公司最想先導入 | 單選 | ✅ | 寫 CODE-RULES／整理範本／PRD 流程／LOOP 驗證 |
| 7 | 建議與想法 | 長文字 | — | 選填，上限 200 字，要顯示剩餘字數 |
| 8 | 願意推薦給同事 | 是／否 | ✅ | — |

系統另外自動記錄：`id`（流水號）、`submittedAt`（送出時間，`YYYY-MM-DD HH:mm`）。

## 4. 行為規則

| # | 規則 |
|---|---|
| BR-1 | 必填沒填不可送出；錯誤訊息用紅字顯示在**該欄位下方**，並聚焦第一個錯誤欄位 |
| BR-2 | 送出成功後顯示成功訊息，表單清空，可以再填下一份 |
| BR-3 | 送出當下按鈕要防重複點擊 |
| BR-4 | 資料存在瀏覽器 `localStorage`，重新整理不會不見 |
| BR-5 | 管理頁的「清空全部回覆」是危險操作，要二次確認，確認鈕要寫明動作 |
| BR-6 | 匯出 CSV 要含 UTF-8 BOM（中文不能亂碼），檔名含日期 |
| BR-7 | 送出的資料層函式要留 `// TODO: DEV 串接 POST /api/survey` 接縫，之後換成真 API 時**呼叫端不用改** |

## 5. 非功能需求

| # | 需求 |
|---|---|
| NFR-1 | 手機（390px）與桌機（1280px）都要能正常填答 |
| NFR-2 | 顏色、間距一律走 design token，禁止硬編碼 hex / px |
| NFR-3 | 全繁體中文；中英文之間加半形空格 |
| NFR-4 | 沒有回覆時，管理頁顯示空狀態，不顯示空表格 |
| NFR-5 | 要能 `pnpm generate` 產出純靜態檔，且發布到 GitHub Pages 子路徑後仍可正常運作 |

## 6. 範圍外（本版不做）

- 後端 API 與資料庫（留接縫即可，下一門課做）
- 登入與權限（管理頁不設密碼，靠網址不外流）
- 編輯或刪除單筆回覆（只做新增、看全部、清空全部）
- 圖表統計（匯出 CSV 後自己用 Excel 看）
